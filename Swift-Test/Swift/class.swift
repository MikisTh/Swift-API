class Music {
  private let name: String

  init(name: String) {
    self.name = name  
  }
}

let music = Music(name: "X")

class Auth {
  private let name: String

  init(name: String) {
    self.name = name  
  }
}

let auth = Auth(name: "X")